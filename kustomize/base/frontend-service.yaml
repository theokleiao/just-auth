apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector:
    component: frontend
  ports:
    - port: 8888
      targetPort: 8888
  type: ClusterIP   # overlays will change to NodePort or LoadBalancer