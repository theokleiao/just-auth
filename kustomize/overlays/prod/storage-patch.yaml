apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
spec:
  storageClassName: fast-ssd   # assume a fast storage class in production
  resources:
    requests:
      storage: 50Gi